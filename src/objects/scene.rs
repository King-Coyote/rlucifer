use crate::objects::*;
use crate::basic_types::*;

pub struct Scene {
    // lights: Vec<Light>,
    // objects: Vec<Object>,
}

impl Scene {
    pub fn new() -> Self {
        Scene {

        }
    }
}